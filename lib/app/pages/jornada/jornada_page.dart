import 'package:flutter/material.dart';

class JornadaPage extends StatelessWidget {
  const JornadaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Jornada Page'),
      ),
      body: Container(),
    );
  }
}
